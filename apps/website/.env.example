# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# When adding additional env variables, the schema in /env/notifications.ts should be updated accordingly

# Prisma
DATABASE_URL='mysql://user:pass@us-east.connect.psdb.cloud/alveusgg?sslaccept=strict'
SHADOW_DATABASE_URL='mysql://user:pass@us-east.connect.psdb.cloud/alveusgg/shadow?sslaccept=strict'

# Next Auth
# You can generate the secret via 'openssl rand -base64 32' on Linux
# More info: https://next-auth.js.org/configuration/options#secret
#NEXTAUTH_SECRET=
NEXTAUTH_URL=https://www.alveus.gg

# Twitch OAuth
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=

# Twitch Event Sub (Webhook)
TWITCH_EVENTSUB_CALLBACK=https://www.alveus.gg/api/webhooks/twitch/eventsub
#   You can generate the secret via 'openssl rand -base64 32' on Linux
TWITCH_EVENTSUB_SECRET=

# Action API secret
#   You can generate the secret via 'openssl rand -base64 32' on Linux
ACTION_API_SECRET=

# OneSignal Push service
ONESIGNAL_APP_ID=
ONESIGNAL_REST_API_KEY=

# OneSignal Push client
NEXT_PUBLIC_ONESIGNAL_APP_ID=
NEXT_PUBLIC_ONESIGNAL_SAFARI_WEB_ID=
NEXT_PUBLIC_ONESIGNAL_SUBDOMAIN=

# Super users
SUPER_USER_IDS=